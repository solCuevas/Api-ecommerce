<h1>{{product.title}}</h1>
<p>{{product.description}}</p>
<p>Precio: ${{product.price}}</p>
<p>Categoría: {{product.category}}</p>
<p>Stock: {{product.stock}}</p>

<form action="/api/carts/1/product/{{product._id}}" method="post">
  <button type="submit">Agregar al carrito</button>
</form>
